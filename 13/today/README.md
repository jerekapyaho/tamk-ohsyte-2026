# Today 10

Added command-line argument handlling using the clap crate.
The --date / -d option specifies a month-day for the event filter.
To get a better idea of the changes required, take a look at the diffs between
src/lib.rs and src/main.rs in directories 09 and 10.
